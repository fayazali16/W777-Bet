# Production site URL (https:// recommended; host-only like localhost:3000 is auto-prefixed). Used for metadata, canonical URLs, JSON-LD, sitemap.
NEXT_PUBLIC_SITE_URL=https://example.com

# Direct link to your Android APK file (https://.../app.apk). Leave unset until the file is hosted.
# NEXT_PUBLIC_APK_URL=
